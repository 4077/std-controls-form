<div class="{__NODE_ID__}" instance="{__INSTANCE__}">

    <div class="table">
        <!-- field -->
        <div class="row">
            <div class="cell label">{LABEL}</div>
            <div class="cell value {CLASS}" hover="hover">{CONTROL}</div>
        </div>
        <!-- / -->
    </div>

</div>
